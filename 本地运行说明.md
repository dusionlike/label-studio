# 本地运行说明

本文档说明如何在当前仓库中启动内部定制版 Label Studio，并让最新前端改动生效。

## 1. 环境前提

- Python 环境已经放在仓库目录下：`.conda`
- 前端依赖已经安装在 `web/node_modules`
- 当前仓库建议使用以下端口：
  - Django：8090
  - 前端热更新：8010

## 2. 直接启动后端

在仓库根目录执行：

```bash
conda activate ./.conda && python label_studio/manage.py runserver 0.0.0.0:8090
```

启动后访问：

```text
http://127.0.0.1:8090
```

## 3. 前端改动如何生效

这个仓库可以用两种方式让前端改动生效。

### 方式 A：构建静态资源（当前机器更稳定）

当前机器的文件监听数量限制较低，`yarn dev` 很容易因为 `ENOSPC: System limit for number of file watchers reached` 失败。

因此建议直接构建前端静态资源：

```bash
cd web
corepack yarn ls:build
```

构建完成后，回到仓库根目录执行：

```bash
cd /home/ubuntu/label-studio
/home/ubuntu/label-studio/.conda/bin/python label_studio/manage.py collectstatic --noinput
```

然后重启 Django：

```bash
/home/ubuntu/label-studio/.conda/bin/python label_studio/manage.py runserver 0.0.0.0:8090
```

### 方式 B：前端热更新开发模式（需要系统放开 watcher 限制）

如果你已经提高了系统的文件监听限制，可以启动前端热更新：

```bash
cd /home/ubuntu/label-studio/web
corepack yarn dev --host 0.0.0.0 --port 8010
```

然后另开一个终端，启动 Django，并把前端地址指向 8010：

```bash
cd /home/ubuntu/label-studio
export FRONTEND_HMR=true
export FRONTEND_HOSTNAME=http://localhost:8010
export DJANGO_HOSTNAME=http://localhost:8090
/home/ubuntu/label-studio/.conda/bin/python label_studio/manage.py runserver 0.0.0.0:8090
```

如果你看到 `ENOSPC` 报错，就不要继续用热更新，改回“方式 A”。

## 4. 本次改动后建议的验证路径

启动成功后，建议按下面路径手动检查：

1. 登录首页。
2. 点击“创建项目”。
3. 检查“项目名称 / 数据导入 / 标注配置”三步页面文案是否为中文。
4. 在“标注配置”里检查模板分组、模板列表、代码/可视化切换、预览按钮和保存按钮是否为中文。
5. 在“数据导入”里上传普通文件，确认页面支持显示 ZIP。
6. 使用 YOLOv8 数据集 ZIP 做一次导入，确认图片和框标注能够进入项目。

## 5. YOLOv8 ZIP 导入说明

当前新增的是 YOLOv8 目标检测 ZIP 导入能力，约束如下：

- 需要 ZIP 中包含 `data.yaml` 或 `data.yml`
- 需要标准的 `images/` 和 `labels/` 目录结构
- 当前按 YOLO 目标检测框格式解析，每行 5 列：`class x_center y_center width height`
- 导入目标项目的标注配置中，至少要有一个 `Image` 和一个 `RectangleLabels`

如果项目配置不满足这个要求，导入会直接报错并提示。

## 6. 已知情况

- 当前环境没有安装 pytest 测试依赖，所以不能直接运行测试命令；如果需要跑测试，先补测试依赖。
- `collectstatic` 过程中会看到一批重复静态文件告警，这在当前仓库里属于已有现象，不会阻止启动。